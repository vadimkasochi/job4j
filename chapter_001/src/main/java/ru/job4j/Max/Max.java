package ru.job4j.Max;

public class Max {


    public int max(int left, int right) {
        return left > right ? left : right;
    }

    public int min(int left, int right) {
        return left < right ? left : right;
    }

    public boolean vomit(int one, int two) {
        return one == two;
    }
}
